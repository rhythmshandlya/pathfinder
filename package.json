{
  "name": "path_finder",
  "version": "1.1.0",
  "private": true,
  "homepage": "http://rhythmshandlya.github.io/pathfinder",
  "dependencies": {
    "gh-pages": "^4.0.0",
    "react": "^16.10.1",
    "react-dom": "^16.10.1",
    "react-scripts": "3.1.2"
  },
  "engines": {
    "npm": ">=6.0.0 <7.0.0",
    "node": ">=14.0.0 <15.0.0"
  },
  "scripts": {
    "start": "react-scripts start --legacy-peer-deps",
    "build": "react-scripts build && cp build/index.html build/404.html",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
